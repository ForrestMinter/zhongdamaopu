# 中大猫谱 · 校园猫咪功能扩展说明

基于 `sysucats/zhongdamaopu` 最新 master 开发，共改动 39 个文件（+3163 行 / -4 行）。
改动以补丁形式交付：`校园猫咪功能扩展.patch`。

## 应用补丁

```bash
cd zhongdamaopu
git checkout -b feature/campus-cat-extensions
git apply 校园猫咪功能扩展.patch
```

## 功能清单

### 1. 领养流程（申请 → 审核 → 状态跟踪）
- **猫咪详情页**：当猫咪状态为「寻找领养中」（adopt=2）时，顶部显示领养横幅，点击进入申请表单。
- **新页面 `pages/adoption/applyAdoption`**：用户填写称呼、联系方式、申请理由；前端校验 + 云端防重复提交。
- **新页面 `pages/info/myAdoption`**：我的申请列表，含完整状态时间线（提交→审核通过/拒绝→可撤销），入口在「我的」页卡片。
- **新页面 `pages/manage/checkAdoption`**：管理员审核台，分「待审核/已通过/未通过」三个标签，支持填写审核备注、一键复制联系方式；**审核通过后自动将猫咪标记为已领养，并自动拒绝同猫其他待审核申请**。入口在管理后台，带待审核数角标。
- 新云函数 `unionOp/adoptionOp.js`，新集合 `adoption`。

### 2. 医疗记录（绝育/就诊/驱虫/伤病时间线）
- 项目原有疫苗记录保持不变，新增通用医疗记录作为补充。
- **新页面 `pages/manage/medicalManage`**：管理员对指定猫咪增删改医疗记录；**添加「绝育」记录时自动更新猫咪的绝育标记**。入口在猫咪详情页「医疗记录」卡片的「管理」按钮（管理员可见）。
- **猫咪详情页**：新增「医疗记录」卡片展示最新一条，点击「查看时间线」弹出完整时间线弹窗。
- 新云函数 `unionOp/medicalOp.js`，新集合 `medical`。

### 3. 喂食打卡（分地区 + 自定义地点 + 零食打卡）
- **分地区打卡主页 `pages/feed/feedHub`**：校区 → 区域 → 猫咪三级组织，每个区域和每只猫显示今日打卡次数（未打卡的排前面，方便查漏补缺），每只猫旁有「喂食」「零食」两个快捷打卡按钮。入口：「我的」页卡片 + 猫咪详情页喂食卡片。
- **打卡页 `pages/feed/feedRecord`**：统计区（喂食总数 / 零食总数 / 近7天 / 近30天）+ 打卡弹窗（日期、时间、地点、备注）+ 记录列表（可删除自己的打卡）。同一用户同一天对同一只猫、同一类别限打卡一次（喂食和零食可以各打一次）。
- **零食打卡独立**：打卡分「喂食 meal / 零食 snack」两类，零食需选择类型（默认猫条、罐头、冻干）；猫咪详情页「喂食动态」卡片有「喂食打卡」「零食打卡」两个入口，统计分开显示。
- **管理员自定义**：新页面 `pages/manage/feedSettings`（管理后台入口），可维护打卡地点列表（打卡页变为下拉选择，另保留「其他」手动输入）和零食类型列表（使用中的类型不可删除）。存储在 `setting` 集合的 `feed_location`、`feed_snack_type` 文档，与疫苗类型同一套管理模式。
- 新云函数 `unionOp/feedOp.js`，新集合 `feed_checkin`（含 feed_category、snack_type、campus、area 字段；老数据自动按喂食类计入）。

### 4. 照片审核：转移猫照 + 批量转移
- 照片审核页每张图新增「转移」标记按钮（紫色）。
- 批量审核时若含转移标记，先弹出猫咪搜索选择器（复用现有 searchCat 组件）选目标猫，确认后自动完成「通过审核 + 转移到目标猫名下」，并重算两只猫的精选照片数。
- 云函数 `unionOp/managePhoto.js` 新增 `transfer` 操作类型。

### 5. 个人主页足迹增强
- **新页面 `pages/info/myFootprint`**，三个标签页：
  - **我拍的照片**：按上传时间倒序，显示猫咪名、上传时间、审核状态，点图可放大，点猫名跳转猫咪主页；
  - **拍过的猫咪**：去重列表，显示封面和「我拍了 N 张」，点击跳转猫咪主页；
  - **我的便利贴**：内容 + 所属猫咪，点击直达该猫的便利贴墙。
- 入口在「我的」页卡片区。

## 数据库变更

新增 3 个集合（EMAS Serverless 会在首次写入时自动创建，无需手动建表）：

| 集合 | 用途 | 关键字段 |
|---|---|---|
| `adoption` | 领养申请 | cat_id, applicant_openid, status(pending/approved/rejected/cancelled), timeline[] |
| `medical` | 医疗记录 | cat_id, type(sterilize/clinic/deworm/injury/other), record_date, hospital |
| `feed_checkin` | 喂食/零食打卡 | cat_id, _openid, feed_category(meal/snack), snack_type, feed_date, feed_time, location, campus, area |
| `setting/feed_location` | 打卡地点配置 | locations: string[]（管理员维护） |
| `setting/feed_snack_type` | 零食类型配置 | types: string[]（管理员维护，默认猫条/罐头/冻干） |

## 权限设计（沿用项目原有体系）

| 操作 | 权限 |
|---|---|
| 提交领养申请 / 喂食打卡 / 查看自己的记录 | 所有登录用户 |
| 领养审核列表 | 管理员等级 ≥ 1 |
| 领养审核操作、医疗记录增删改 | 管理员等级 ≥ 2（与疫苗管理一致） |
| 照片转移 | 管理员等级 ≥ 1（与照片审核一致） |

## 部署步骤

1. 应用补丁后，重新上传部署云函数 `unionOp`（整个函数目录有 3 个新文件 + 2 个修改文件）。
2. 微信开发者工具重新编译上传小程序端。
3. 建议先在体验版验证以下路径：
   - 把一只猫的领养状态改为「寻找领养中」→ 详情页出现领养横幅 → 提交申请 → 管理后台审核通过 → 猫变为「已领养」；
   - 管理后台「喂食设置」添加地点和零食类型 → 「我的」页「喂食打卡」进入分地区主页 → 给某只猫分别打喂食卡、零食卡 → 统计与今日计数刷新；
   - 管理员在详情页进入医疗记录管理，添加一条绝育记录 → 猫的绝育标签变化；
   - 照片审核页标记「转移」→ 批量审核 → 选择目标猫；
   - 「我的」→ 我的足迹 三个标签页。

## PR 提交建议

- 分支名：`feature/campus-cat-extensions`
- 建议先给原仓库开 issue 说明这批功能（维护者在 README 留了 QQ 群 956808218，也可以先群里沟通），再提 PR 时关联该 issue。
- commit 信息可按功能拆 5 个，例如：`feat: 领养申请与审核流程`、`feat: 医疗记录时间线`、`feat: 喂食打卡与统计`、`feat: 照片审核支持批量转移`、`feat: 我的足迹页面`。

## 未做的事（如需要可继续）

- 领养/医疗/喂食的订阅消息通知（项目现有 sendMsgV2 可复用，需在微信后台申请新模板）。
- 喂食排行榜（可复用现有 leaderboard 页面模式）。
- 医疗记录照片附件。
